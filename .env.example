# App (used on the server inside Docker)
SESSION_SECRET=CHANGEME
DOMAIN=localhost

# Local development
DB_PATH=db/gleam_mcp_todo.db
PORT=8080

# Deployment (used locally by bin/deploy and bin/setup-server)
DEPLOY_HOST=your-server.example.com
DEPLOY_USER=deploy
REPO_URL=git@github.com:your-org/gleam-mcp-todo.git
